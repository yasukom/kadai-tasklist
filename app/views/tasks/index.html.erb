<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : タスク: <%= task.content %> ステータス: <%= task.status %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの投稿', new_task_path %>